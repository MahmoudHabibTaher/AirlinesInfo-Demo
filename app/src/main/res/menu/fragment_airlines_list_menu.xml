<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search"
        android:title="@string/search_menu_item_title"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="collapseActionView|ifRoom"/>

    <item
        android:id="@+id/action_filter"
        android:icon="@drawable/ic_filter_list"
        android:title="@string/filter_menu_item_title"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/action_filter_all"
                android:title="@string/filter_all_menu_item_title"/>
            <item
                android:id="@+id/action_filter_favorites"
                android:title="@string/filter_favorites_menu_item_title"/>
        </menu>

    </item>
</menu>